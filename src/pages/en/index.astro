---
import { changeLanguage } from "i18next";
import Layout from "@layouts/Layout.astro";
import Navbar from "@components/theme/navbar/Navbar.astro";
import homeJson from "@data/pages/home.json";

changeLanguage("en");
---

<Layout
  pageTitle={homeJson.pageTitle}
  pageDescription={homeJson.pageDescription}
  pageKeywords={homeJson.pageKeywords}
  pageAuthor={homeJson.pageAuthor}
/>
